Example: Frontend task with Planning Mode and signals preference.

Prompt:
"Planning Mode: I want to add a data-driven list component in Angular v22 that fetches items from `/api/items`, uses signals for state, and supports pagination and filtering. Provide a short plan, the component code (standalone), a service using `HttpClient` with RxJS/observables as needed, and unit tests. Prefer signals and reactive patterns throughout."