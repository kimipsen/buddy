Example: Frontend task with Planning Mode and signals preference.

Prompt:
"Planning Mode: I want to add a data-driven list component in Angular v22 that fetches items from `/api/items`, uses signals for state, supports pagination and filtering, and uses TailwindCSS for layout and styling. Provide a short plan plus visual artifacts: a wireframe and a proposed screenshot/mockup (include file paths like `src/frontend/docs/wireframe-list.svg` and `src/frontend/docs/mockup-list.svg`, alt text, and captions). Store images under `src/frontend/docs/`. Then provide the component code (standalone) with Tailwind utility classes, a service using `HttpClient` with RxJS/observables as needed, unit tests, and brief Tailwind integration steps for Angular v22. Prefer signals and reactive patterns throughout."
Secrets: Use a `.env` file for any local development secrets (do not commit `.env`; include `src/frontend/.env.example` with placeholders). When possible, avoid client-side secrets; prefer backend storage and injection via CI/CD.